{
  "name": "interview-prep-platform",
  "private": true,
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "index:parse": "npm run index:parse --prefix backend",
    "index:classify": "npm run index:classify --prefix backend",
    "index:build": "npm run index:build --prefix backend",
    "index:all": "npm run index:all --prefix backend",
    "test:backend": "npm test --prefix backend"
  }
}
