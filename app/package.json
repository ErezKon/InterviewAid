{
  "name": "interview-prep-platform",
  "private": true,
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "index:parse": "npm run index:parse -w backend",
    "index:classify": "npm run index:classify -w backend",
    "index:build": "npm run index:build -w backend",
    "index:all": "npm run index:all -w backend",
    "test:backend": "npm test -w backend"
  }
}
